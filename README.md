# unlit3d_asset_files

Snapshot images for the `wgpu_unlit_render` GPU integration tests.

Referenced from the main repository at `tests/snapshots` (a submodule symlinked
into place), and compared with the SSIMULACRA2 perceptual metric.
